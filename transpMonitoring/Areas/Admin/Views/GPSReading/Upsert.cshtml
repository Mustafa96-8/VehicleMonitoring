@model GPSReading
@{

    ViewData["Title"] = "Запись данных с GPS";
}
<style>
    input::-webkit-outer-spin-button,
    input::-webkit-inner-spin-button {
        -webkit-appearance: none;
        margin: 0;
    }

    input[type="number"] {
        -moz-appearance: textfield;
    }
</style>
<div class="d-flex justify-content-center">
    <h2 class="text-primary ">@(Model.Id != 0 ? "Изменить" : "Создать") запись с датчика GPS</h2>
    <hr />
</div>
<form method="post">
    <input asp-for="Id" hidden />
    <div class="d-flex justify-content-center">
        <div class="border d-flex flex-column w-50">
            <div class="p-3 ">
                <label asp-for="GPSDataId" class="p-0"></label>
                <input asp-for="GPSDataId" class="form-control" />
                <span asp-validation-for="GPSDataId" class="text-danger"></span>
            </div>
            <div class="p-3 ">
                <label asp-for="latitude" class="p-0"></label>
                <input asp-for="latitude" class="form-control" type="number" />
                <span asp-validation-for="latitude" class="text-danger"></span>
            </div>
            <div class="p-3">
                <label asp-for="longitude" class="p-0"></label>
                <input asp-for="longitude" class="form-control" type="number" />
                <span asp-validation-for="longitude" class="text-danger"></span>
            </div>
            <div class="p-3 ">
                <label asp-for="altitude" class="p-0"></label>
                <input asp-for="altitude" class="form-control" type="number" />
                <span asp-validation-for="altitude" class="text-danger"></span>
            </div>
            <div class="p-3 ">
                <label asp-for="speed" class="p-0"></label>
                <input asp-for="speed" class="form-control" type="number" />
                <span asp-validation-for="speed" class="text-danger"></span>
            </div>
            <div class="p-3 ">
                <label asp-for="heading" class="p-0"></label>
                <input asp-for="heading" class="form-control" type="number" />
                <span asp-validation-for="heading" class="text-danger"></span>
            </div>
            <div class=" d-flex flex-lg-row justify-content-start  ">
                <div class="p-3">
                    @if (@Model.Id != 0)
                    {
                        <button type="submit" class="btn btn-outline-primary">Update</button>
                    }
                    else
                    {
                        <button type="submit" class="btn btn-outline-primary">Create</button>
                    }
                </div>
                <div class="p-3">
                    <a asp-controller="GPSReading" asp-action="Index" class="btn btn-outline-primary" style="padding-block-end">
                        К списку записей
                    </a>
                </div>
                @if (@Model.Id != 0)
                {
                    <div class="p-3">
                        <a asp-controller="GPSReading" asp-action="Delete" asp-route-id="@Model.Id" class="btn btn-outline-danger" style="padding-block-end">
                            Удалить
                        </a>
                    </div>
                }
            </div>
        </div>
    </div>

</form>

@section Scripts {
    @{
        <partial name="_ValidationScriptsPartial" />
    }
}