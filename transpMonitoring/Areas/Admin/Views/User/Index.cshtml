@model List<User>
@{

    ViewData["Title"] = "Список пользователей";
}
<h2>@ViewData["Title"]</h2>
<div class="container">
    <div class="container d-flex justify-content-end">
        <a asp-controller="User" asp-action="Upsert" type="button" class="btn btn-outline-primary btn">
            <i class="bi bi-plus-circle"></i> Зарегестрировать пользователя
        </a>
    </div>
    <table class="table table-light table-striped table-hover m-3 ">

        <thead class="tab-content">
            <tr>
                <th>
                    Логин
                </th>
                <th>
                    Имя
                </th>
                <th>
                    Фамилия
                </th>
                <th>
                    Роль
                </th>
                <th>

                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var obj in Model.OrderBy(u => u.Id))
            {
                <tr>
                    <td class="tab-content">@obj.Login</td>
                    <td class="tab-content">@obj.FirstName</td>
                    <td class="tab-content">@obj.LastName</td>
                    <td class="tab-content">@obj.Role</td>

                    <td class="tab-content ">
                        <div class="w-75 btn-group" role="group">
                            <a asp-controller="User" asp-action="Upsert" asp-route-id="@obj.Id" class="btn btn-primary mx-1 rounded-2">
                                <i class="bi bi-pencil-square"></i> Изменить
                            </a>
                            <a asp-controller="User" asp-action="Delete" asp-route-id="@obj.Id" class="btn btn-danger mx-1 rounded-2">
                                <i class="bi bi-trash3"></i> Удалить
                            </a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
